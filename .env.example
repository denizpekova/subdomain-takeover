# === Subdomain Takeover Configuration ===
# This file serves as a template. Copy to '.env' to use.

# Number of concurrent threads to use (Example)
# THREAD_COUNT=50

# Default custom wordlist path 
# WORDLIST_PATH=./subdomains.txt

# External DNS Provider API Keys (if integrated later)
# DNS_API_KEY="your_api_key_here"
